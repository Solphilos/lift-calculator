const calcWeekOne = (max) => {
    let setOne = .65 * max;
    let setTwo = .75 * max;
    let setThree = .85 * max;
   console.log(setOne, setTwo, setThree)
 }
 

 const calcWeekTwo = (max) => {
    let setOne = .70 * max;
    let setTwo = .80 * max;
    let setThree = .90 * max;
   console.log(setOne, setTwo, setThree)
 }

 const calcWeekThree = (max) => {
    let setOne = .75 * max;
    let setTwo = .85 * max;
    let setThree = .95 * max;
   console.log(setOne, setTwo, setThree)
 }
 
 const calcWeekOne = (max) => {
    let setOne = .40 * max;
    let setTwo = .50 * max;
    let setThree = .60 * max;
   console.log(setOne, setTwo, setThree)
 }
 
 
 